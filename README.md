# pandemic_SIRD

Per settare CMake:

`cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug`

Per buildare:

`cmake --build build`

Per eseguire il programma:

`./build/equation`
`./build/agent`

Per eseguire i tests:

`./build/equation.test`
`./build/agent.test`
